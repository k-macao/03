diff --git a/.github/workflows/m.yml b/.github/workflows/m.yml
index 0eef427..4cba046 100644
--- a/.github/workflows/m.yml
+++ b/.github/workflows/m.yml
@@ -7,11 +7,13 @@
 #   3. 每天北京时间 09:00 定时   → 自动重建站点 + 推送完整报告到微信 (无需手动操作)
 #
 # 动态抓取管线 (每次运行均执行, 真正上线 — 行情+社区双动态):
-#   market_data.py      → market_data.json      (Yahoo Finance / Stooq 多源回退抓取最新行情)
+#   market_data.py      → market_data.json      (Yahoo Finance / Stooq 多源回退抓取最新行情 + 动态技术面)
+#   macro_data.py       → macro_data.json       (FRED / 世界银行 / 东方财富宏观指标 + 人工维护项, 02 节唯一数据源;
+#                                                每项带 as_of 与 max_age_days, 取不到即 --mock 回放并标注陈旧)
 #   community_data.py   → community_data.json   (14 大社区 HTTP GET + 动态模板回退，每次刷新当天日期)
 #   tools/probe_sentiment_apis.py → docs/sentiment-api-eval.md (量化平台舆情接口实测评分)
 #   sentiment_factors.py → sentiment_data.json (聚宽/米筐/掘金/优矿 + 免费兜底源舆情·新闻因子；境内取不到则 --mock 降级)
-#   build_site.py       → report.html           (把行情+社区+抓取日期注入页面与推送内容)
+#   build_site.py       → report.html           (把行情+社区+宏观+时效核对表注入页面与推送内容)
 #   wechat_push.py --embed                       (把最新推送负载内嵌进 report.html)
 #   wechat_push.py --emit _site/wechat.json
 #   wechat_push.py --push --scheduled            (推送前日期宽松校验, 单品行情/社区失败不阻断)
@@ -55,8 +57,20 @@ jobs:
       - name: 🌐 动态抓取行情 (market_data.py → market_data.json)
         run: python3 market_data.py
 
+      - name: 🌍 动态抓取宏观 (macro_data.py → macro_data.json · FRED/世界银行/东方财富)
+        run: |
+          set +e
+          timeout 300 python3 macro_data.py --timeout 8
+          if python3 -c "import json,sys;d=json.load(open('macro_data.json',encoding='utf-8'));i=d.get('indicators') or {};sys.exit(0 if sum(1 for r in i.values() if r.get('status')=='live')>=5 else 1)"; then
+            echo "✅ 宏观层：联网取到 ≥5 项实时指标（02 节网页与微信推送同源渲染）"
+          else
+            echo "⚠️ 宏观层：部分源不可达 → 降级离线回放 --mock（tests/fixtures 快照），正文按 as_of 逐项标注陈旧"
+            python3 macro_data.py --mock
+          fi
+          set -e
+
       - name: 🗣️ 动态抓取社区 (community_data.py → community_data.json · 14 源)
-        run: python3 community_data.py
+        run: python3 community_data.py --macro-data macro_data.json
 
       - name: 🔬 舆情接口实测 (tools/probe_sentiment_apis.py → docs/sentiment-api-eval.md)
         run: python3 tools/probe_sentiment_apis.py --mock
@@ -128,8 +142,20 @@ jobs:
       - name: 🌐 动态抓取行情 (market_data.py)
         run: python3 market_data.py
 
+      - name: 🌍 动态抓取宏观 (macro_data.py → macro_data.json · FRED/世界银行/东方财富)
+        run: |
+          set +e
+          timeout 300 python3 macro_data.py --timeout 8
+          if python3 -c "import json,sys;d=json.load(open('macro_data.json',encoding='utf-8'));i=d.get('indicators') or {};sys.exit(0 if sum(1 for r in i.values() if r.get('status')=='live')>=5 else 1)"; then
+            echo "✅ 宏观层：联网取到 ≥5 项实时指标（02 节网页与微信推送同源渲染）"
+          else
+            echo "⚠️ 宏观层：部分源不可达 → 降级离线回放 --mock（tests/fixtures 快照），正文按 as_of 逐项标注陈旧"
+            python3 macro_data.py --mock
+          fi
+          set -e
+
       - name: 🗣️ 动态抓取社区 (community_data.py · 14 源)
-        run: python3 community_data.py
+        run: python3 community_data.py --macro-data macro_data.json
 
       - name: 🔬 舆情接口实测 (tools/probe_sentiment_apis.py → docs/sentiment-api-eval.md)
         run: python3 tools/probe_sentiment_apis.py --mock
@@ -181,8 +207,20 @@ jobs:
       - name: 🌐 动态抓取行情 (market_data.py)
         run: python3 market_data.py
 
+      - name: 🌍 动态抓取宏观 (macro_data.py → macro_data.json · FRED/世界银行/东方财富)
+        run: |
+          set +e
+          timeout 300 python3 macro_data.py --timeout 8
+          if python3 -c "import json,sys;d=json.load(open('macro_data.json',encoding='utf-8'));i=d.get('indicators') or {};sys.exit(0 if sum(1 for r in i.values() if r.get('status')=='live')>=5 else 1)"; then
+            echo "✅ 宏观层：联网取到 ≥5 项实时指标（02 节网页与微信推送同源渲染）"
+          else
+            echo "⚠️ 宏观层：部分源不可达 → 降级离线回放 --mock（tests/fixtures 快照），正文按 as_of 逐项标注陈旧"
+            python3 macro_data.py --mock
+          fi
+          set -e
+
       - name: 🗣️ 动态抓取社区 (community_data.py · 14 源)
-        run: python3 community_data.py
+        run: python3 community_data.py --macro-data macro_data.json
 
       - name: 🔬 舆情接口实测 (tools/probe_sentiment_apis.py → docs/sentiment-api-eval.md)
         run: python3 tools/probe_sentiment_apis.py --mock
